core/build/models/
core/build/.venv-tse/
.worktrees/
scripts/__pycache__/
.env

# Icon build outputs (regenerated by assets/icons/build.sh)
assets/icons/generated/

# Claude Code session state (per-user). Skills are project-scoped and
# meant to be shared, so we explicitly un-ignore .claude/skills/.
.claude/*
!.claude/skills/
.idea/

# superpowers brainstorming session state (per-session mockups + server info)
.superpowers/